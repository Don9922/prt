-- CREATE TABLE
CREATE TABLE Employee (
    emp_id     INT PRIMARY KEY,
    name       VARCHAR(50),
    department VARCHAR(50),
    salary     INT
);

-- INSERT
INSERT INTO Employee VALUES (1, 'Alice',   'HR',      50000);
INSERT INTO Employee VALUES (2, 'Bob',     'IT',      70000);
INSERT INTO Employee VALUES (3, 'Charlie', 'IT',      80000);
INSERT INTO Employee VALUES (4, 'Diana',   'HR',      45000);
INSERT INTO Employee VALUES (5, 'Eve',     'Finance', 60000);

-- AGGREGATE FUNCTIONS
SELECT COUNT(*) AS total_employees FROM Employee;
SELECT SUM(salary)  AS total_salary FROM Employee;
SELECT AVG(salary)  AS avg_salary   FROM Employee;
SELECT MAX(salary)  AS max_salary   FROM Employee;
SELECT MIN(salary)  AS min_salary   FROM Employee;

-- GROUP BY
SELECT department, COUNT(*)       AS total_employees FROM Employee GROUP BY department;
SELECT department, SUM(salary)    AS total_salary    FROM Employee GROUP BY department;
SELECT department, AVG(salary)    AS avg_salary      FROM Employee GROUP BY department;

-- HAVING
SELECT department, SUM(salary)  AS total_salary     FROM Employee GROUP BY department HAVING SUM(salary)  > 90000;
SELECT department, COUNT(*)     AS total_employees  FROM Employee GROUP BY department HAVING COUNT(*) > 1;